

/**
 * @description Component for creating page title for the top of each page.
 * @param {*} param0 
 */
export default function PageTitle({title}) {
    

    return (
        <h1 style={'pagetitle-title'}>{title}</h1>
    )
}